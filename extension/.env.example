# Lead Generator Chrome extension — local dev config
# Copy to `.env.local` (this folder) or set as env vars before running `npm run dev:backend`.

# Known dev extension ID (Chrome assigns this on first load). If you re-load the
# extension in a different profile or after clearing chrome data, regenerate.
LEADGEN_EXTENSION_ID=hahcoepljmhmiaomolmfhbmpahnfbpep