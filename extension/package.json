{
  "name": "lead-generator-google-maps-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension that captures Google Maps results into Lead Generator.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "typescript": "^5.4.5"
  }
}
